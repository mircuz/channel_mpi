#include <mpi.h>
#include <math.h>
#include <stdlib.h>
#include <time.h>
#include "remap3d_wrap.h"

INTEGER rank
USE dnsdata
USE initialization
WRITE



USE convol_trasp
!//y_pencil_parser_mom( VV, start_mode_x, end_mode_x, start_mode_z, end_mode_z, 0)
y_pencil_parser_vel( V, start_mode_x, end_mode_x, start_mode_z, end_mode_z, 0)

<* 	free(u);	free(v);	free(w);	free(u_read);	free(v_read);	free(w_read);
  	free(uu);	free(uv);	free(vv);	free(vw);	free(ww);	free(uw); 	*>
<*	MPI_Finalize();	*>

